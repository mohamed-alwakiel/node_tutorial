// ConmmonJS, every file is module (by default)
// Modules - Encapsulation Code (only share a minimum)

const names = require("./4-name-module");
const sayHi = require("./5-utils-module");
const data = require("./6-alternative-flavor");

require('./7-mind-grenade')

// sayHi("test");
// sayHi(names.wiko);
// sayHi(names.looz);
